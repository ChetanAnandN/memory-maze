/**
 * Index Page
 * 
 * This is the main entry point for the Virtual Memory Simulator application.
 * It renders the main simulator component which contains all the interactive
 * demonstrations of virtual memory management concepts.
 */

import VirtualMemorySimulator from '@/components/simulator/VirtualMemorySimulator';

const Index = () => {
  return <VirtualMemorySimulator />;
};

export default Index;
